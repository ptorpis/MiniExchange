# MiniExchange

README UNDER CONSTRUCTION...

Linux support only.

See `docs/build-help.md`, or run:

```bash
python3 -m scripts.build_helper -i
```

to get help building the executables.

Requires: 
- a Python package manager (textual, numpy, pandas, matplotlib)
- Pybind11
- gcc/clang (only tested with gcc 13.0)
- GoogleTest

> [!NOTE]
> The Python prototype of this project has been moved to branch `legacy`

